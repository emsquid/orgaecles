<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/hour_view"
    android:layout_width="match_parent"
    android:layout_height="75dp"
    android:background="@color/white">

    <View
        android:id="@+id/hour_divider"
        android:layout_width="0dp"
        android:layout_height="1.5dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="8dp"
        android:background="@color/lighter_fir_green"
        app:layout_constraintBottom_toBottomOf="@+id/hour_label"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/hour_label"
        app:layout_constraintTop_toTopOf="@+id/hour_label" />

    <TextView
        android:id="@+id/hour_label"
        android:layout_width="45sp"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="12dp"
        android:gravity="end"
        android:textAlignment="textEnd"
        android:textColor="@color/lighter_fir_green"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="12:00" />
</androidx.constraintlayout.widget.ConstraintLayout>
